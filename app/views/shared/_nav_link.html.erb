<% active ||= false %>
<%= link_to text,
path,
class:
  "block rounded-xl px-2 py-4 " +
    (
      if active
        "bg-white/10 text-orange-400"
      else
        "text-slate-200 hover:bg-white/5 hover:text-emerald-200"
      end
    ) %>
